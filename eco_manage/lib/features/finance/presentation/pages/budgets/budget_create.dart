import 'package:flutter/material.dart';

class BudgetCreate extends StatelessWidget {
  const BudgetCreate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Budget Create'),
      ),
    );
  }
}